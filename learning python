print("Yousef's CAPM Calculator")

x = float(input('Enter Risk Free Rate: '))/100
y = float(input('Enter Beta: '))
z = float(input('Enter Market Return: '))/100

r = x+(y*(z-x))
rp = float(r)*100

print("Expected return in decimal terms is "+str(r))
print ("or in percentage terms "+str(rp)+"%")
